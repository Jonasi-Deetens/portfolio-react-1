import React from 'react'
import { HomeBanner } from '../webcomponents/HomeBanner';

const Home = () => {
    return (
        <div>
            < HomeBanner />
        </div>
    );
}

export default Home;